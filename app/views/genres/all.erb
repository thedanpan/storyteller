<h4>All Genres:</h4>
<div class="genres_list">
  <ul>
    <% genres.each do |genre| %>
    <li><a href="/genre/<%= genre.id %>"><%= genre.name.capitalize %></a><span style="font-size:0.75em; float: right">(<%= genre.stories.count %>)</span></li>
    <% end %>
  </ul>
</div>
